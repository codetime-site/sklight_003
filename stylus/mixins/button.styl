btnmx( bg = clrx(primary2), clr = clrx(wht), pdt = pdtx(bm), pdl = pdlx(bs) )
    font-size: fsx(btn)
    font-weight: bold
    border-radius: radx(btn)
    border: none
    padding: pdt pdl
    background-color: bg 
    color: clr
    transition: all .5s ease

    &:hover
        background-color: clrx()
        color: clrx(primary)
        
