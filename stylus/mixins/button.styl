btn( bg = clr(primary), clr = clr(wht) )
    padding pad(btn_top) pad(btn_left)
    background: bg
    color clr
    border-radius: var(--rad_btn);
    font-size: fs(menu)
    font-weight: bold
    border: none

    &:hover
        background: clr(wht)
        color clr(primary)
        
