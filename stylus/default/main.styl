html {
  box-sizing: border-box;
}

*, *::before, *::after {
  box-sizing: inherit;
}
a{
    text-decoration: none;
    color: white;
}
a:hover{
    color: #d68c50;
}

header
    width 100%

.btn
    btn()


.container
    cont()
    